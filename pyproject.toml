[project]
name = "openwrt-ax9000-builder"
version = "0.1.0"
description = "Local web controller for OpenWrt NSS firmware builds."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
]

[tool.uv]
package = false
